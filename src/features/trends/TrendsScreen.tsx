import { ScreenPlaceholder } from '../../components/ScreenPlaceholder.tsx'

export function TrendsScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Patterns"
      title="Trends"
      description="Summaries and visualizations will live here after the tracking foundations are complete."
    />
  )
}
